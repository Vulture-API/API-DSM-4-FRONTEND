# TEMPORÁRIO: destino server-side usado pelo rewrite do Next.
API_SERVER_URL=http://localhost:3000

# Base relativa usada no navegador. Não aponta diretamente para o backend.
NEXT_PUBLIC_API_BASE_URL=/api/backend
